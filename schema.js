// database structure
export const AuInfo = `CREATE TABLE if not exist aut_data(
    first_name text not null,
    last_name text  not null,
    phone_number int not null,
    address varchar(255),
)`




